% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/json.R
\name{json_lapply}
\alias{json_lapply}
\title{Apply a function over elements of a JSON array or object}
\usage{
json_lapply(x, f, ...)
}
\arguments{
\item{x}{A \link{json_array} or \link{json_object}.}

\item{f}{A function.  For \link{json_array} it receives each element; for
\link{json_object} it receives each member value.  The return value must be
something accepted by the respective constructor — a plain R scalar,
vector, list, \code{NULL}, or a \link{json} object.}

\item{...}{Additional arguments passed to \code{f}.}
}
\value{
A \link{json_array} or \link{json_object} (same class as \code{x}) whose
elements/members are the results of calling \code{f}.
}
\description{
Applies \code{f} to each element of a \link{json_array} or each member value of a
\link{json_object} and returns a new JSON object of the same type with the
transformed values.  Works like \link[base:lapply]{base::lapply} but preserves the JSON
container type.
}
\examples{
# Scale every number in an object
obj <- json_object(a = 1, b = 2, c = 3)
json_lapply(obj, function(v) v * 2)

# Wrap plain values as typed json objects
arr <- json_array(1, "hello", TRUE)
json_lapply(arr, to_json)
}
