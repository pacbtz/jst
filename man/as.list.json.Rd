% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/json.R
\name{as.list.json}
\alias{as.list.json}
\title{Coerce a JSON object to a plain R value}
\usage{
\method{as.list}{json}(x, ...)
}
\arguments{
\item{x}{A \link{json} object.}

\item{...}{Unused.}
}
\value{
An R value corresponding to the JSON type.
}
\description{
Recursively converts a \link{json} object back to ordinary R values:
\itemize{
\item \link{json_null}    -> \code{NULL}
\item \link{json_boolean} -> \code{logical(1)}
\item \link{json_number}  -> \code{numeric(1)}
\item \link{json_string}  -> \code{character(1)}
\item \link{json_vector}  -> atomic vector (character / integer / double / logical)
\item \link{json_array}   -> unnamed \code{list} (elements recursively converted)
\item \link{json_object}  -> named \code{list} (member values recursively converted)
}
}
\examples{
as.list(json_object(a = 1, b = "hello", c = TRUE))
as.list(json_array(1, "two", json_null()))
}
